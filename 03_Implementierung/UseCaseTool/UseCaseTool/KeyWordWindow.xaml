<Window x:Class="UseCaseTool.KeyWordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UseCaseTool"
        mc:Ignorable="d"
        Title="Schlüsselwortübersicht" Height="400" Width="500" WindowStartupLocation="CenterOwner">
    <Grid>
        <DataGrid x:Name="keyWordGrid" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" AutoGenerateColumns="False"  CanUserResizeColumns="True"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn  Header="Deutsch" IsReadOnly="True" Binding="{Binding Item1, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn  Header="Englisch" IsReadOnly="True" Binding="{Binding Item2, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
